import { NextResponse } from 'next/server';

export async function GET() {
  // Placeholder route until model backend is integrated
  return NextResponse.json({ results: [] });
}

export async function POST() {
  // Same here
  return NextResponse.json({ results: [] });
}
